---
title: "API Introduction"
description: "Getting started with the InventPay API"
---

## Welcome to the InventPay API

The InventPay API allows you to integrate cryptocurrency payment processing into your applications. Our RESTful API is designed to be simple, predictable, and easy to integrate.

<CardGroup cols={2}>
  <Card title="RESTful Design" icon="globe">
    Standard HTTP methods and status codes
  </Card>
  <Card title="JSON Format" icon="brackets-curly">
    All requests and responses use JSON
  </Card>
  <Card title="Comprehensive SDKs" icon="code">
    Official libraries for JavaScript and Python
  </Card>
  <Card title="Real-time Webhooks" icon="bell">
    Instant notifications for all events
  </Card>
</CardGroup>

## Base URL

All API requests should be made to:

```
https://api.inventpay.io
```

## Authentication

The InventPay API uses API keys for authentication. Include your API key in the `X-API-Key` header of every request.

### Get Your API Key

1. Sign up at [inventpay.io](https://inventpay.io)
2. Navigate to [Dashboard → Settings → API Keys](https://inventpay.io/dashboard/settings/api-keys)
3. Click "Generate New API Key"
4. Copy and securely store your API key

<Warning>
  Never expose your API key in client-side code or public repositories. Always
  use environment variables to store sensitive credentials.
</Warning>

### Withdrawal API Key

Programmatic withdrawals require an additional `X-Withdrawal-Key` header. This is a separate key from your main API key, disabled by default for security.

1. Go to **Dashboard → Settings → Withdrawal API Key**
2. Click **"Generate Key"** to enable programmatic withdrawals
3. Include both headers when creating withdrawals:

```bash
curl -X POST https://api.inventpay.io/v1/merchant/withdrawal/create \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "X-Withdrawal-Key: YOUR_WITHDRAWAL_KEY" \
  -H "Content-Type: application/json" \
  -d '{"amount": 100, "currency": "USDT_BEP20", "destinationAddress": "0x..."}'
```

<Info>
  Dashboard withdrawals use 2FA instead and do not need a withdrawal API key.
  You can revoke the withdrawal key at any time to instantly disable all API-based withdrawals.
</Info>

### Example Authentication

<CodeGroup>

```bash cURL
curl https://api.inventpay.io/v1/merchant/balance \
  -H "X-API-Key: cmfnz7jei0005p54eekx4e6mb"
```

```javascript JavaScript
const response = await fetch("https://api.inventpay.io/v1/merchant/balance", {
  headers: {
    "X-API-Key": "cmfnz7jei0005p54eekx4e6mb",
  },
});
```

```python Python
import requests

response = requests.get(
    'https://api.inventpay.io/v1/merchant/balance',
    headers={'X-API-Key': 'cmfnz7jei0005p54eekx4e6mb'}
)
```

</CodeGroup>

## Request Format

### HTTP Methods

The API uses standard HTTP methods:

| Method   | Usage                     |
| -------- | ------------------------- |
| `GET`    | Retrieve resources        |
| `POST`   | Create new resources      |
| `PUT`    | Update existing resources |
| `DELETE` | Remove resources          |

### Content Type

All requests with a body must include the `Content-Type: application/json` header.

### Example Request

```bash
curl -X POST https://api.inventpay.io/v1/create_payment \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 29.99,
    "amountCurrency": "USD",
    "currency": "USDT_BEP20",
    "orderId": "order-12345"
  }'
```

## Response Format

All API responses are returned in JSON format with a consistent structure.

### Successful Response

```json
{
  "success": true,
  "data": {
    // Response data here
  },
  "message": "Operation completed successfully",
  "statusCode": 200
}
```

### Error Response

```json
{
  "success": false,
  "error": "Error message",
  "details": {
    // Additional error details
  },
  "statusCode": 400
}
```

## HTTP Status Codes

The API uses standard HTTP status codes:

| Code | Meaning               | Description                   |
| ---- | --------------------- | ----------------------------- |
| 200  | OK                    | Request successful            |
| 201  | Created               | Resource created successfully |
| 400  | Bad Request           | Invalid request parameters    |
| 401  | Unauthorized          | Invalid or missing API key    |
| 404  | Not Found             | Resource not found            |
| 429  | Too Many Requests     | Rate limit exceeded           |
| 500  | Internal Server Error | Server error occurred         |

## Error Handling

### Error Response Structure

```json
{
  "success": false,
  "error": "Validation failed",
  "details": {
    "currency": "Invalid currency. Must be one of: BTC, ETH, LTC, USDT_ERC20, USDT_BEP20, SOL, USDC_SOL, USDC_BEP20"
  },
  "statusCode": 400
}
```

### Common Errors

<AccordionGroup>
  <Accordion title="401 Unauthorized" icon="lock">
    **Cause:** Invalid or missing API key
    
    **Solution:** Verify your API key is correct and included in the `X-API-Key` header
    
    ```json
    {
      "success": false,
      "error": "Invalid API key",
      "statusCode": 401
    }
    ```
  </Accordion>

  <Accordion title="400 Bad Request" icon="circle-exclamation">
    **Cause:** Invalid request parameters
    
    **Solution:** Check the `details` field for specific validation errors
    
    ```json
    {
      "success": false,
      "error": "Validation failed",
      "details": {
        "amount": "Amount must be greater than 0"
      },
      "statusCode": 400
    }
    ```
  </Accordion>

  <Accordion title="429 Rate Limit Exceeded" icon="gauge-high">
    **Cause:** Too many requests in a short time
    
    **Solution:** Implement exponential backoff and respect rate limits
    
    ```json
    {
      "success": false,
      "error": "Rate limit exceeded",
      "retryAfter": 60,
      "statusCode": 429
    }
    ```
  </Accordion>

  <Accordion title="404 Not Found" icon="magnifying-glass">
    **Cause:** Resource doesn't exist
    
    **Solution:** Verify the resource ID is correct
    
    ```json
    {
      "success": false,
      "error": "Payment not found",
      "statusCode": 404
    }
    ```
  </Accordion>
</AccordionGroup>

## Rate Limits

To ensure fair usage and system stability, the API implements rate limiting:

| Endpoint Type    | Limit        | Window     |
| ---------------- | ------------ | ---------- |
| Payment Creation | 100 requests | Per minute |
| Status Checks    | 300 requests | Per minute |
| Withdrawals      | 10 requests  | Per minute |
| Other Endpoints  | 60 requests  | Per minute |

### Rate Limit Headers

All responses include rate limit information:

```
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 95
X-RateLimit-Reset: 1640000000
```

### Handling Rate Limits

<CodeGroup>

```javascript JavaScript
async function makeRequest(url, options) {
  const response = await fetch(url, options);

  if (response.status === 429) {
    const retryAfter = response.headers.get("X-RateLimit-Reset");
    const waitTime = retryAfter - Date.now();

    await new Promise((resolve) => setTimeout(resolve, waitTime));
    return makeRequest(url, options); // Retry
  }

  return response;
}
```

```python Python
import time

def make_request(url, headers):
    response = requests.get(url, headers=headers)

    if response.status_code == 429:
        retry_after = int(response.headers.get('X-RateLimit-Reset'))
        wait_time = retry_after - int(time.time())

        time.sleep(wait_time)
        return make_request(url, headers)  # Retry

    return response
```

</CodeGroup>

## Pagination

Endpoints that return lists support pagination using query parameters:

| Parameter | Type    | Default | Description              |
| --------- | ------- | ------- | ------------------------ |
| `page`    | integer | 1       | Page number              |
| `limit`   | integer | 10      | Items per page (max 100) |

### Example

```bash
GET /v1/payments?page=2&limit=20
```

### Response

```json
{
  "success": true,
  "data": {
    "items": [...],
    "pagination": {
      "page": 2,
      "limit": 20,
      "total": 150,
      "pages": 8
    }
  }
}
```

## Idempotency

To safely retry requests without performing the same operation twice, use idempotency keys:

```bash
curl -X POST https://api.inventpay.io/v1/create_payment \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "X-Idempotency-Key: unique-key-123" \
  -H "Content-Type: application/json" \
  -d '{...}'
```

<Info>
  Idempotency keys are stored for 24 hours. Use a unique key for each unique
  request.
</Info>

## Versioning

The API version is included in the URL path:

```
https://api.inventpay.io/v1/...
```

**Current Version:** v1

We maintain backwards compatibility within a major version. Breaking changes will result in a new major version (v2, v3, etc.).

## SDKs and Libraries

<CardGroup cols={2}>
  <Card title="JavaScript/TypeScript" icon="js" href="/sdks/javascript/installation">
    Official Node.js and browser SDK with full TypeScript support
    
    ```bash
    npm install inventpay
    ```
  </Card>

  <Card title="Python" icon="python" href="/sdks/python/installation">
    Official Python SDK with type hints and async support
    
    ```bash
    pip install inventpay
    ```
  </Card>
</CardGroup>

## Webhooks

For real-time event notifications, configure webhooks:

<Steps>
  <Step title="Configure Webhook URL">
    Set your endpoint in the dashboard or via API
  </Step>
  <Step title="Verify Signatures">
    Always verify webhook signatures for security
  </Step>
  <Step title="Handle Events">Process payment and withdrawal events</Step>
  <Step title="Return 200 OK">Respond quickly to acknowledge receipt</Step>
</Steps>

<Card title="Webhook Documentation" icon="webhook" href="/webhooks/overview">
  Learn how to set up and secure webhooks
</Card>

## Testing

### Test Mode

Use separate test API keys for development and testing:

- Test payments don't process real cryptocurrency
- Test all integration flows safely
- Generate test API keys in dashboard

### Test Data

Use these test values for development:

| Field    | Test Value               |
| -------- | ------------------------ |
| Amount   | Any positive number      |
| Currency | All supported currencies |
| Order ID | Any string               |

<Warning>
  Never use production API keys in test environments or public repositories.
</Warning>

## Best Practices

<AccordionGroup>
  <Accordion title="Secure Your API Keys" icon="key">
    - Store keys in environment variables
    - Never commit keys to version control
    - Rotate keys periodically
    - Use separate keys for different environments
  </Accordion>

{" "}
<Accordion title="Handle Errors Gracefully" icon="triangle-exclamation">
  - Always check response status codes - Parse error messages for details -
  Implement retry logic with exponential backoff - Log errors for debugging
</Accordion>

{" "}
<Accordion title="Use Webhooks" icon="webhook">
  - Don't poll for status changes - Implement webhook signature verification -
  Handle duplicate deliveries - Return 200 OK quickly
</Accordion>

{" "}
<Accordion title="Implement Idempotency" icon="fingerprint">
  - Use idempotency keys for critical operations - Store request IDs to prevent
  duplicates - Handle retries safely
</Accordion>

  <Accordion title="Monitor Your Integration" icon="chart-line">
    - Track API response times
    - Monitor error rates
    - Set up alerts for failures
    - Review webhook delivery success rates
  </Accordion>
</AccordionGroup>

## API Endpoints

### Payments

<CardGroup cols={2}>
  <Card title="Create Payment" icon="plus" href="/api-reference/create-payment">
    Create a fixed-currency payment request
  </Card>
  <Card
    title="Create Invoice"
    icon="file-invoice"
    href="/api-reference/create-invoice"
  >
    Create a multi-currency invoice
  </Card>
  <Card
    title="Get Payment Status"
    icon="circle-info"
    href="/api-reference/get-payment-status"
  >
    Check payment status and confirmations
  </Card>
</CardGroup>

### Balances

<CardGroup cols={2}>
  <Card title="Get Balances" icon="wallet" href="/api-reference/get-balances">
    Retrieve all cryptocurrency balances
  </Card>
  <Card
    title="Get Balance by Currency"
    icon="coins"
    href="/api-reference/get-balance-by-currency"
  >
    Get specific currency balance
  </Card>
</CardGroup>

### Withdrawals

<CardGroup cols={2}>
  <Card
    title="Create Withdrawal"
    icon="arrow-right-from-bracket"
    href="/api-reference/create-withdrawal"
  >
    Withdraw funds to external wallet
  </Card>
  <Card
    title="Get Withdrawal"
    icon="magnifying-glass"
    href="/api-reference/get-withdrawal"
  >
    Check withdrawal status
  </Card>
</CardGroup>

## Support

<CardGroup cols={2}>
  <Card title="Documentation" icon="book" href="/introduction">
    Browse complete documentation
  </Card>
  <Card
    title="Email Support"
    icon="envelope"
    href="mailto:support@inventpay.io"
  >
    Contact our support team
  </Card>
  <Card title="API Status" icon="signal" href="https://status.inventpay.io">
    Check current API status
  </Card>
  <Card
    title="GitHub Issues"
    icon="github"
    href="https://github.com/inventpay/inventpay-sdk"
  >
    Report bugs or request features
  </Card>
</CardGroup>

## Next Steps

<Steps>
  <Step title="Get Your API Key">Sign up and generate your API key</Step>
  <Step title="Try the Quickstart">Create your first payment in 5 minutes</Step>
  <Step title="Install an SDK">Choose JavaScript or Python SDK</Step>
  <Step title="Set Up Webhooks">Configure real-time notifications</Step>
  <Step title="Go Live">Switch to production API keys</Step>
</Steps>

<Card title="Quickstart Guide" icon="rocket" href="/quickstart">
  Get started in under 5 minutes
</Card>
